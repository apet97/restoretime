node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.*
!.env.example
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
coverage/

# install-capture tool runtime state (encrypted installation records, key)
tools/install-capture/var/

# Planning session transcripts (not blueprint inputs)
omp-session-*.html

# Agent worktrees (harness-managed, never part of the product)
.claude/

# Live-run state (encrypted installation records + the codec key). Never committed.
var/
